name: base1 para actions

on: 
  push:
    branches:
      - main
    paths:
      - 'teste/**'   # so roda quando push no diretorio teste

jobs:
  info:
    runs-on: ubuntu-lastest
    steps:
      - name: Gera saida do github context
        run: echo "${{ toJSON(github) }}"

